language: cpp
dist: xenial
compiler: gcc
sudo: required

matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
      env:
        - MATRIX_EVAL="CC=gcc-8 && CXX=g++-8"

before_install:
    - eval "${MATRIX_EVAL}"
    - sudo apt-get install -y cmake libboost-all-dev
    - ./install-catch2.sh

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make -j
  - test/tests
